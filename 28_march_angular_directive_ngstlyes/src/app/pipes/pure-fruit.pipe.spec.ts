import { PureFruitPipe } from './pure-fruit.pipe';

describe('PureFruitPipe', () => {
  it('create an instance', () => {
    const pipe = new PureFruitPipe();
    expect(pipe).toBeTruthy();
  });
});
