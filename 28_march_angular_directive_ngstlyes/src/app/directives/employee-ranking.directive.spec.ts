import { EmployeeRankingDirective } from './employee-ranking.directive';

describe('EmployeeRankingDirective', () => {
  it('should create an instance', () => {
    const directive = new EmployeeRankingDirective();
    expect(directive).toBeTruthy();
  });
});
