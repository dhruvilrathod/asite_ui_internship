import { ProductCreationDirective } from './product-creation.directive';

describe('ProductCreationDirective', () => {
  it('should create an instance', () => {
    const directive = new ProductCreationDirective();
    expect(directive).toBeTruthy();
  });
});
