import { PurepipePricePipe } from './purepipe-price.pipe';

describe('PurepipePricePipe', () => {
  it('create an instance', () => {
    const pipe = new PurepipePricePipe();
    expect(pipe).toBeTruthy();
  });
});
