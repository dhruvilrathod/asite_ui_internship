import { ImpurepipeJsonStringPipe } from './impurepipe-json-string.pipe';

describe('ImpurepipeJsonStringPipe', () => {
  it('create an instance', () => {
    const pipe = new ImpurepipeJsonStringPipe();
    expect(pipe).toBeTruthy();
  });
});
